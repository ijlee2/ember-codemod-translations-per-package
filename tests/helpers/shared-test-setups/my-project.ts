import type { CodemodOptions, Options } from '../../../src/types/index.js';

const codemodOptions: CodemodOptions = {
  projectRoot: 'tmp/my-addon',
};

const options: Options = {
  projectRoot: 'tmp/my-addon',
};

export { codemodOptions, options };
